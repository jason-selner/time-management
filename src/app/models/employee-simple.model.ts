import { Roles } from '../../utilities/Constants';

export class EmployeeSimple {
  UserId: number;
  Roles: Roles[];

  FullName: string;
  Address: string;
  EmailAddress: string;
  JobTitle: string;
}
